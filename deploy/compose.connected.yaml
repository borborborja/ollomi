# Explicit opt-in: for LAN AI servers, external OpenAI-compatible providers,
# local connectors, and the initial Ollama model download.
services:
  api:
    networks:
      private:
        aliases: [ollomi-api]
      inference:
  worker:
    networks:
      private:
      inference:
  ollama:
    networks:
      private:
      inference:
